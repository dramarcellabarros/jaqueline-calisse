// Placeholder para futuras interações (menu mobile, analytics, formulário).
// Nenhuma dependência externa por enquanto — site 100% estático.

document.addEventListener('DOMContentLoaded', () => {
  // Reservado para: tracking de clique nos CTAs de WhatsApp, se/quando
  // Analytics (GA4) for configurado — ver README > Pendências.
});
